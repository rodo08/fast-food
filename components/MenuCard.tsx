import React from "react";
import { Text, View } from "react-native";

const MenuCard = () => {
  return (
    <View>
      <Text>MenuCard</Text>
    </View>
  );
};

export default MenuCard;
